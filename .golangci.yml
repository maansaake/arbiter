linters:
  disable-all: true
  enable:
  # default linters
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
  # non-default additions
    - bodyclose
    - errname
    - gocritic
    - gofumpt
    - goimports
    - misspell
